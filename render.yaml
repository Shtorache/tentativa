services:
  - type: web
    name: gerenciador-financeiro
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn finance_manager.wsgi:application
